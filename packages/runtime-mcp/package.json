{
  "name": "@local-runtime/mcp",
  "version": "0.1.0",
  "description": "MCP server for the local development runtime manager.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "runtime-mcp": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "pnpm build && node --test 'test/**/*.test.mjs'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
